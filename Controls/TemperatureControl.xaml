<UserControl x:Class="Hardware_Monitor.Controls.TemperatureControl"
             x:Name="rootControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hardware_Monitor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="350">
    <Grid Background="Transparent" ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ShowGridLines="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="0.2*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                Grid.Column="0"
                Text="{Binding LeftTopText, ElementName=rootControl}"
                HorizontalAlignment="Stretch"
                TextAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource TemperatureTitleStyle}"/>

            <TextBlock Grid.Column="1"
                Text="{Binding Temperature, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalAlignment="Stretch"
                TextAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource HourMinuteStyle}"/>
            
            <TextBlock Grid.Row="0"
                Grid.Column="2"
                Text="°C"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource TemperatureTitleStyle}"/>

        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="9*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>

            <local:BarControl Grid.Column="1"
                Value="{Binding Temperature, ElementName=rootControl}"
                Height="20"
                VerticalAlignment="Center"
                HorizontalAlignment="Stretch"/>
        </Grid>

    </Grid>
</UserControl>

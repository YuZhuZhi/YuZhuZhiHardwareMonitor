<UserControl x:Class="Hardware_Monitor.Controls.LoadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hardware_Monitor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <local:DashBoardControl Grid.Row="0"
                                Value="{Binding LoadValue, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

        <TextBlock Grid.Row="0" Style="{StaticResource ExtraInfoStyle}"
           Text="{Binding ExtraInfo, RelativeSource={RelativeSource AncestorType=UserControl}}"
           VerticalAlignment="Bottom" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Style="{StaticResource LoadTitleStyle}"
                   Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" />
    </Grid>
</UserControl>
